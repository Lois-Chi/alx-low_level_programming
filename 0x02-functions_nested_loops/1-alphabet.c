#include <main.h>

/**
 * main - prints the alphabets in lowercase
 * followed by a new line
 * Return: Always 0 (Success)
 */

int main(void)


{
	_putchar ch;

	for (ch 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	_putchar ('\n');
	return (0);
}
